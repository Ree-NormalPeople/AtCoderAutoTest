# paiza.IO 言語仕様メモ / 免責事項

このファイルは、`README.md` とは別に、
paiza.IO API 利用時の言語情報と公開時の注意事項をまとめたものです。

## 1. この拡張で使用している paiza 言語キー

この拡張（`content.js`）で選択可能な言語は以下です。

- `python3` (Python 3)
- `cpp` (C++)
- `c` (C)
- `javascript` (JavaScript)

## 2. 言語仕様の取り扱い（重要）

paiza.IO API の `guest` 利用では、以下のような制約があります。

- コンパイラやランタイムの**厳密バージョン固定指定**はできません
- CPUアーキテクチャ（例: `x86_64`）を明示指定できません
- AtCoderと同一環境（例: `C++23 (gcc, x86_64)`）を再現できません

そのため、AtCoderでACのコードでも paiza.IO では `CE/RE/TLE` になる場合があります。

## 3. 実行時に取得している主な項目

この拡張は paiza API から以下を利用しています。

- `stdout`
- `stderr`
- `time`
- `memory`
- `build_stdout`
- `build_stderr`
- `build_result`

表示実装は `content.js` を参照してください。

## 4. 判定ロジック（本拡張）

本拡張の判定は簡易判定です。

- `build_result === "failure"` -> `CE`
- `status === "timeout"` または `time > 2.0` -> `TLE`
- `stderr` がある -> `RE`
- `stdout.trim() === expected.trim()` -> `AC`
- それ以外 -> `WA`

## 5. 免責事項（GitHub公開向け）

- 本ツールは学習・補助目的で提供されます。正確性・可用性・継続性を保証しません。
- paiza.IO / AtCoder 側の仕様変更により、予告なく動作しなくなる可能性があります。
- 本ツールの利用によって生じた直接・間接の損害について、作者は責任を負いません。
- AtCoder・paiza.IO の利用規約に反しない範囲で使用してください。
- 外部APIのレート制限・障害・混雑の影響を受ける場合があります。
- この拡張機能はAI（GitHub Copilot）を使用して作成されていることに留意してください。拡張機能を使う上で生成AIは使われていないので、AtCoderの利用規約には違反しないはずです。

## 6. プライバシーに関する注意

- コードと標準入力はブラウザの `localStorage` に保存されます（問題URL単位）。
- 実行時には入力コード・標準入力が paiza.IO API に送信されます。
- 機密情報・秘密鍵・個人情報を**絶対に**コードや入力に含めないでください。機密情報・秘密鍵・個人情報を含んだ情報が第三者に漏洩しても、作者は責任を負いません。負いたくないです。